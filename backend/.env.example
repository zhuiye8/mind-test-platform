NODE_ENV=development
PORT=3001

# 前端地址（供 CORS 与分享链接使用）
CORS_ORIGIN=https://localhost:3000
CLEAR_PROXY=true

# 数据库（默认连接本地 Docker 实例）
POSTGRES_USER=psychology_user
POSTGRES_PASSWORD=psychology_password
POSTGRES_DB=psychology_test_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=dev-jwt-secret-change-me
JWT_EXPIRES_IN=7d

# AI 服务（本地多模态服务）
AI_SERVICE_URL=http://localhost:5678
AI_SERVICE_TOKEN=dev-fixed-token-2024
AI_REPORT_BACKEND_ONLY=true
AI_REQUIRED=false
AI_AUTOSTART_RTSP=true
MEDIAMTX_HOST=http://192.168.0.94:8889

# 语音合成 & 大模型
BAIDU_TTS_TOKEN=your-baidu-tts-token
LLM_API_BASE=https://api.deepseek.com
LLM_API_KEY=sk-f56e0b5f45524c56931a5c8b5d170078
LLM_MODEL=deepseek-chat
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2000
LLM_TIMEOUT_MS=60000
LLM_USE_CHAT=true

REPORT_API_KEY=
