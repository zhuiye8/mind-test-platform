# 前端环境变量配置示例

# API基础URL（生产环境需要修改）
VITE_API_BASE_URL=http://localhost:3001/api

# AI服务配置（可选，用于调试和开发环境直连）
# 注意：正式环境下前端会通过后端API获取AI服务配置，无需设置此项
# VITE_AI_SERVICE_URL=http://192.168.0.204:5000
# VITE_WEBSOCKET_URL=ws://192.168.0.204:5000/socket.io/

# 其他可选环境变量
# VITE_APP_TITLE=心理测试平台
# VITE_BUILD_MODE=production

# WebSocket连接配置（调试用）
# VITE_WS_DEBUG=true           # 启用WebSocket调试日志
# VITE_WS_RETRY_TIMES=5        # WebSocket重试次数
# VITE_WS_TIMEOUT=10000        # WebSocket连接超时（毫秒）